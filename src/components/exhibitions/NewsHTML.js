import React, { Component } from "react";
import { Text, View } from "react-native";
import HTMLView from "react-native-htmlview";

export default class NewsHTML extends Component {
  render() {
    return (
      <View>
        <Text />
      </View>
    );
  }
}
